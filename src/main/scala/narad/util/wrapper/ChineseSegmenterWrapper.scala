package narad.util
//import edu.stanford.nlp.process.ChineseDocumentToSentenceProcessor

/*
object ChineseSegmenterWrapper {
	
	def segment(str: String): Array[String] = {
		val segmenter = new ChineseDocumentToSentenceProcessor
		val sents = new ArrayBuffer[String]
		for (sentence <- segmenter.fromPlainText(str)) {
			sents += sentence
		}
		return sents.toArray
	}
}
*/