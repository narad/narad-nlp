package narad.projects.otlearn
import scala.collection.mutable.ArrayBuffer

//case class Instance(label: Boolean, word: PhoneticWord) {
//	val isCorrect = label
//}

//extends Iterable[PhoneticWord]{
//	val label = label
//}
/*
	val features = new ArrayBuffer[Feature]
	
  def += (feature: Feature) = {
		features += feature
	}
		
	override def toString: String = "%s:\t%s".format(label, features.map(f => "%s-%s".format(f.label, f.weight)).mkString(", "))
}

*/